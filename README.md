# Лабораторна робота №1  
## Тема: Класи та об’єкти 

### Виконав:
- **Студент:** Кранго Ігнат Андрійович  
- **Група:** 622п  
- **Освітня програма:** 121 Інженерія програмного забезпечення  
- **Прийняв:** Доц. Лучшев П.О.

---

## Мета роботи
- Навчитися аналізувати предметну область і виявляти класи.  
- Використовувати принцип абстракції для виділення характеристик і поведінки класів.  
- Реалізовувати класи мовою **C#**.  
- Створювати і працювати з об’єктами класів.  

---

## Завдання
Розробити консольну програму, яка:
1. Описує клас із приватними та публічними полями.  
2. Використовує **enum** та не менше 4–5 різних типів даних.  
3. Містить методи, що описують поведінку класу.  
4. Реалізує валідацію введених даних.  
5. Дозволяє зберігати `N` об’єктів (визначається користувачем).  
6. Має меню з пунктами:  
   - `1` – Додати об’єкт  
   - `2` – Переглянути всі об’єкти  
   - `3` – Знайти об’єкт  
   - `4` – Продемонструвати поведінку  
   - `5` – Видалити об’єкт  
   - `0` – Вийти з програми  

---

## Опис класу `Product`

### Характеристики:
- **Name** (`string`) – назва продукту  
- **Category** (`enum Category`) – категорія продукту  
- **Price** (`decimal`) – ціна  
- **Stock** (`int`) – кількість на складі  

### Enum `Category`:
- Electronics  
- Clothes  
- Food  
- Furniture  
- Cosmetics  

---

## Валідація характеристик

### Name:
- довжина від **3 до 30 символів**;  
- не допускається порожній рядок.  

### Price:
- значення повинно бути числом;  
- діапазон: **0.01 – 100000**.  

### Stock:
- тільки цілі числа;  
- діапазон **0 – 10000**.  

### Category:
- повинна відповідати одному зі значень переліку enum.  

---

## Поведінка класу (методи)
- **restock(int amount)** – додати товар на склад  
- **purchase(int amount)** – придбати товар  
- **discount(double percent)** – застосувати знижку  

---

## Тестування

Проведено тестування всіх пунктів меню:

### Додавання
- Коректні та некоректні значення  
- Перевищення максимальної кількості об’єктів  

### Перегляд
- Порожній список  
- Відображення таблиці з даними  

### Пошук
- За назвою та за категорією  
- Обробка випадків, коли об’єкт не знайдено  

### Поведінка
- Додавання на склад  
- Спроба придбання більшої кількості, ніж є  
- Некоректні знижки  

### Видалення
- За номером  
- За категорією  

---

## Висновок

У ході виконання роботи було:  
- проаналізовано предметну область;  
- розроблено клас `Product` та enum `Category`;  
- реалізовано три методи поведінки;  
- здійснено валідацію даних користувача;  
- реалізовано повноцінне меню керування об’єктами;  
- проведено тестування на коректність і стійкість програми.

Робота демонструє практичне застосування принципів ООП у мові **C#**.

---

## Програмна реалізація класу

```csharp
public class Product
{
    private string _name;
    private Category _category;
    private decimal _price;
    private int _stock;

    public string Name
    {
        get => _name;
        set => _name = value;
    }

    public Category Category
    {
        get => _category;
        set => _category = value;
    }

    public decimal Price
    {
        get => _price;
        set => _price = value;
    }

    public int Stock
    {
        get => _stock;
        set => _stock = value;
    }

    public void restock(int amount)
    {
        if (amount <= 0)
            throw new ArgumentException("Кiлькiсть має бути бiльшою за 0.");

        _stock += amount;
    }

    public void purchase(int amount)
    {
        if (amount <= 0)
            throw new ArgumentException("Кiлькiсть має бути бiльшою за 0.");

        if (amount > _stock)
            throw new InvalidOperationException("Недостатньо товару на складi.");

        _stock -= amount;
    }

    public void discount(double percent)
    {
        if (percent < 0 || percent > 90)
            throw new ArgumentException("Знижка має бути в межах 0–90%.");

        _price -= _price * (decimal)(percent / 100);
    }

    public override string ToString()
    {
        return $"{Name,-20} | {Category,-12} | {Price,8:F2} | {Stock,5}";
    }
}
